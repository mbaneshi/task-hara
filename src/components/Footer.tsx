import { Box } from "@mui/system";
export default function Footer() {
  return (
    <Box
      sx={{
        width: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>Footer</h1>
    </Box>
  );
}
